/** @type {import('next').NextConfig} */
const config = {
  transpilePackages: [
    "@studio/ui",
    "@studio/utils",
    "@studio/ai-core",
    "@studio/tailwind-config",
  ],
};

export default config;
